import test from'node:test';import assert from'node:assert/strict';import{parseArgs,applyOptions}from'../src/cli.js';import{DEFAULT_SETTINGS}from'../src/config/settings.js';import{quickProfile}from'../src/menu/index.js';
test('parseArgs separates flags, values and positionals',()=>{const o=parseArgs(['--max','30','--no-tunnel','extra','--debug']);assert.equal(o.values.get('max'),'30');assert.ok(o.flags.has('no-tunnel'));});
test('parseArgs rejects options with a missing value',()=>assert.throws(()=>parseArgs(['--max']),/Missing value/));
test('applyOptions overrides settings without mutating the source',()=>{const n=applyOptions(DEFAULT_SETTINGS,parseArgs(['--max','5','--tunnel-limit','2','--no-streaming']));assert.equal(n.scan.maxCandidates,5);assert.equal(n.tunnel.limit,2);assert.equal(n.streaming.enabled,false);});
test('applyOptions validates numeric input',()=>assert.throws(()=>applyOptions(DEFAULT_SETTINGS,parseArgs(['--max','abc'])),/positive number/));
test('lowest-variant flag is explicit and the abr alias remains compatible',()=>{assert.equal(applyOptions(DEFAULT_SETTINGS,parseArgs(['--lowest-variant'])).streaming.variantMode,'lowest');assert.equal(applyOptions(DEFAULT_SETTINGS,parseArgs(['--abr'])).streaming.variantMode,'lowest');});
test('quickProfile reduces the workload for a fast run',()=>assert.ok(quickProfile(DEFAULT_SETTINGS).scan.maxCandidates<DEFAULT_SETTINGS.scan.maxCandidates));
