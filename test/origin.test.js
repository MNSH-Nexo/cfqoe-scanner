import test from 'node:test'; import assert from 'node:assert/strict'; import { serveOrigin } from '../src/origin/server.js';
async function start(){const server=await serveOrigin({host:'127.0.0.1',port:0});const base=`${'http'+'://'}127.0.0.1:${server.address().port}`;return{server,base}}
test('probe origin serves health, bounded downloads and HLS',async()=>{const{server,base}=await start();try{assert.equal((await fetch(`${base}/healthz`).then(r=>r.json())).ok,true);assert.equal((await fetch(`${base}/__down?bytes=4096`).then(r=>r.arrayBuffer())).byteLength,4096);assert.match(await fetch(`${base}/cfqoe/stream/master.m3u8`).then(r=>r.text()),/EXT-X-STREAM-INF/);assert.equal((await fetch(`${base}/__up`,{method:'POST',body:Buffer.alloc(1024)}).then(r=>r.json())).bytes,1024);}finally{await new Promise(resolve=>server.close(resolve));}});
